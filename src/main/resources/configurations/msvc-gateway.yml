eureka:
  client:
    register-with-eureka: false

server:
  port: 8080

spring:
  application:
    name: msvc-gateway
  cloud:
    gateway:
      mvc:
        routes:
          - id: authentication
            uri: http://localhost:8090
            predicates:
              - Path=/api/auth/**